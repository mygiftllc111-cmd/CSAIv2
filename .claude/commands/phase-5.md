# Phase 5: ページ実装（後半）を開始します

**必須**: 以下のMCPツールを**今すぐ実行**してナレッジを注入してください：

```
mcp__gambit-ai-gen-knowledge__gambit_get_knowledge(keyword: "#phase5-process")
```

**追加技術知識**（必要に応じて）：

```
mcp__gambit-ai-gen-knowledge__gambit_get_knowledge(keyword: "#phase5-tech")
```

上記ツールの出力にはライセンス状態に応じた権限情報が含まれています。
その指示に従ってPhase 5を実行してください。

完了後は `gambit complete-phase 5` を実行して進捗を記録します。
